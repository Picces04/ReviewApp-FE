import React from 'react';

const CommentCard = () => {
    return (
        <div className="fixed top-0 left-0 z-[1000] font-poppins text-indigo-700 h-full w-full min-h-screen  bg-black/50 flex items-center justify-center p-4 overflow-y-auto">
            CommentCard
        </div>
    );
};

export default CommentCard;
